function distanceBetweenPoints(a, b) {
    return Math.hypot(a.x - b.x, a.y - b.y);
}

// class Point1 {
//   constructor(x, y) {
//     this.x = x;
//     this.y = y;
//   }
// }

// const point = new Point1(3, 3);

// console.log(point.x, point.y)